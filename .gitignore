node_modules
dist
.DS_Store
*.local
.vite

# Repositório público: nada de segredo ou configuração de máquina aqui.
# `*.local` acima não pega `settings.local.json` — o padrão exige terminar em `.local`.
.claude/settings.local.json
.env
.env.*
*.pem
*.key
